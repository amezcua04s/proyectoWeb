@{
    ViewData["Title"] = "Especialidades Médicas"; // Título más general
    Layout = "~/Views/Shared/_Layout.cshtml";
    bool isLoggedIn = ViewBag.IsLoggedIn ?? false; // Obtener el estado del login
}

@section Styles {
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.0/font/bootstrap-icons.css">
    <style>
        .doctor-card {
            transition: transform 0.3s ease;
        }

            .doctor-card:hover {
                transform: translateY(-5px);
            }
    </style>
}

@if (isLoggedIn)
{
    <div class="container my-5">
        <h1 class="text-center mb-4">Nuestros Especialistas</h1>

        <div class="row mb-4">
            <div class="col-md-6 mx-auto">
                <select id="filtroEspecialidad" class="form-select shadow-sm">
                    <option value="todas" selected>Todas las especialidades</option>
                    @foreach (var esp in new List<string> { "Anestesiólogo", "Cardiólogo", "Pediatra", "Cirujano", "Neurólogo", "Ortopédico", "Urólogo", "Psiquiatra" })
                    {
                        <option value="@esp">@esp</option>
                    }
                </select>
            </div>
        </div>

        <div class="row" id="doctor-list">
        </div>
    </div>

    <div class="modal fade" id="doctorModal" tabindex="-1" aria-hidden="true">
    </div>
}
else
{
    <div class="container my-5">
        <h1 class="text-center mb-4">Nuestras Especialidades Médicas</h1>
        <p class="text-center mb-4">Descubre la amplia gama de servicios y profesionales especializados que el Hospital Johnson tiene para ofrecerte. Para agendar una cita, por favor inicia sesión.</p>

        <div class="row mt-4 justify-content-center">
            <div class="col-md-4 mb-4">
                <div class="card h-100 shadow-sm text-center p-3">
                    <i class="bi bi-heart-pulse-fill display-4 text-danger mb-3"></i>
                    <h5 class="card-title">Cardiología</h5>
                    <p class="card-text">Expertos en salud cardiovascular y prevención de enfermedades del corazón.</p>
                </div>
            </div>
            <div class="col-md-4 mb-4">
                <div class="card h-100 shadow-sm text-center p-3">
                    <i class="bi bi-baby display-4 text-info mb-3"></i>
                    <h5 class="card-title">Pediatría</h5>
                    <p class="card-text">Cuidado médico especializado para bebés, niños y adolescentes.</p>
                </div>
            </div>
            <div class="col-md-4 mb-4">
                <div class="card h-100 shadow-sm text-center p-3">
                    <i class="bi bi-person-circle display-4 text-warning mb-3"></i>
                    <h5 class="card-title">Dermatología</h5>
                    <p class="card-text">Diagnóstico y tratamiento de enfermedades de la piel, cabello y uñas.</p>
                </div>
            </div>
            <div class="col-md-4 mb-4">
                <div class="card h-100 shadow-sm text-center p-3">
                    <i class="bi bi-brain display-4 text-primary mb-3"></i>
                    <h5 class="card-title">Neurología</h5>
                    <p class="card-text">Especialistas en trastornos del sistema nervioso, incluyendo cerebro y columna.</p>
                </div>
            </div>
            <div class="col-md-4 mb-4">
                <div class="card h-100 shadow-sm text-center p-3">
                    <i class="bi bi-bone display-4 text-success mb-3"></i>
                    <h5 class="card-title">Ortopedia</h5>
                    <p class="card-text">Diagnóstico y tratamiento de problemas en huesos, articulaciones y músculos.</p>
                </div>
            </div>
        </div>

        <div class="text-center mt-5">
            <p>¿Interesado en agendar una cita o conocer a nuestros doctores?</p>
            <a asp-controller="Account" asp-action="Login" class="btn btn-primary btn-lg me-3">Iniciar Sesión</a>
            <a asp-controller="Account" asp-action="Register" class="btn btn-outline-primary btn-lg">Registrarse</a>
        </div>
    </div>
}

@section Scripts {
    <script src="~/js/doctores.js"></script>
    <script>
        document.addEventListener('DOMContentLoaded', function() {
            // Solo inicializa el script de doctores si el usuario está logueado
            // y si el doctores.js contiene una función de inicialización que deba llamarse
            @{
                    if (isLoggedIn)
                    {
                            // Asume que doctores.js tiene una función como initDoctorsList()
                            // o que se ejecuta automáticamente. Si doctores.js espera que el DOM
                            // ya tenga los elementos del filtro y la lista de doctores,
                            // esta parte lo inicializará solo cuando estén presentes.
                            <text>
                                if (typeof initializeDoctorsList === 'function') { // Si tienes una función para inicializar
                                    initializeDoctorsList();
                                } else {
                                    // Si doctores.js se ejecuta automáticamente al cargar el DOM,
                                    // o si solo necesita que los elementos estén presentes
                                    console.log('Script doctores.js cargado para usuario logueado.');
                                }
                            </text>
                    }
            }
            console.log('Página de especialidades cargada.');
        });
    </script>
}